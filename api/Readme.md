# API

## Helpful commands

```
npx sequelize-cli db:migrate
npx sequelize-cli db:migrate:undo
npx sequelize-cli migration:generate --name some-name
npx sequelize-cli model:generate --name ModelName --attributes filename:string

npm run build
npm run start
```