services:
  pantry-api:
    build: ./api
    ports:
      - 4300:4300
    env_file:
      - stack.env
    volumes:
      - pantry_data:/app/data
  pantry-ui:
    build: ./ui/pantry-ui
    ports:
      - 4200:4200
    environment:
      - API_BASEURL=${API_BASEURL}
      - SITE_TITLE=${SITE_TITLE}

volumes:
  pantry_data:
