version: '2'

services:
  pantry-ui:
    build:
      context: https://github.com/kevdog114/pantry.git
      dockerfile: ui/pantry-ui/Dockerfile
      args:
        - api_baseurl=http://helloworld
    pull_policy: build
    ports:
      - 4200:4200