(function (window) {
    window.__env = window.__env || {};

    // API url
    window.__env.apiUrl = 'http://localhost:4300';

    // Site Name
    window.__env.siteName = 'My Pantry Site (Dev)';

}(this));
